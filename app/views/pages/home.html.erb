<!DOCTYPE html>
<html>
<head>
  <title>Welcome to Medical App</title>
  <%=stylesheet_link_tag 'home.css' %>
</head>
<body>
<% if user_signed_in? %>
  <div class="container">
    <p class="welcome-message">Hello! <%= current_user.doctor? ? 'Doctor' : 'Receptionist' %></p>
    <% if current_user.doctor? %>
      <p><%= link_to 'View Patients', patients_path, class: "button" %></p>
      <p><%= link_to 'View Registration Graph', doctors_registration_graph_path, class: "button" %></p>
    <% else %>
      <p><%= link_to 'Register Patients', new_patient_path, class: "button" %></p>
      <p><%= link_to 'View Patients', patients_path, class: "button" %></p>
    <% end %>
    <p><%= link_to 'Logout', destroy_user_session_path, class: "button", method: :delete %></p>
  </div>
<% else %>
  <div class="container">
    <h1>Welcome to Medical App</h1>
    <p>This is a medical application for doctors and receptionists.</p>
    <p><%= link_to 'Sign Up', new_user_registration_path, class: "button" %></p>
    <p><%= link_to 'Login', new_user_session_path, class: "button" %></p>
  </div>
<% end %>
</body>
</html>
